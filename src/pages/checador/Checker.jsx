import Clock from "../../components/clock/Clock";
import "./Checker.scss";

const Checker = () => {
  return (
    <section className="checker">
      <Clock />
    </section>
  );
};

export default Checker;
